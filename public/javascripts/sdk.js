// This script simulates what an SDK would do when loaded
document.getElementById('sdk-output').className = 'ran';
document.getElementById('sdk-output').textContent = 'SDK script executed — injected by loader.';
