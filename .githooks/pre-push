#!/bin/sh
CHANGED=$(git diff --name-only @{u} HEAD 2>/dev/null || git diff --name-only $(git merge-base HEAD origin/main) HEAD)
if [ -n "$CHANGED" ]; then
  echo "$CHANGED" | xargs npx biome check --write
fi
npm run typecheck && npm run build